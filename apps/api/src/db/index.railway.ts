// TODO: Railway production database
// Install: drizzle-orm/node-postgres + pg
//
// import { drizzle } from 'drizzle-orm/node-postgres';
// import * as schema from './schema.js';
//
// const db = drizzle(process.env.DATABASE_URL!, { schema });
//
// Migrations run via `drizzle-kit migrate` in the deploy pipeline,
// not on startup like the SQLite driver.

export {};
